# INPUT
distance = float(input('Type the distance traveled(km): '))
fuel = float(input('Enter the amount of fuel used(l): '))
# MATH
total = distance / fuel
# OUTPUT
print('\n Average consumption(km/l):{} \n'.format(total))
